자바스크립트 코드 작성이 가능한 위치
- HTML 태그의 이벤트 리스너 속성
- <script></script> 태그에 작성
- 자바스크립트 파일에 작성
- URL에 작성?

-> 자바스크립트 코드를 별도 파일에 작성 - .js파일에 저장, <script> 태그 없이 코드만 저장
  => <script src="~.js">에서 src로 불러오기 때문에

Alert() 함수

자바스크립트 데이터 타입
-숫자
-논리
-문자열
- 객체 레퍼런스 타입
- null

prompt는 무조건 입력받은 값을 String 형으로 인식

함수
- 익명 함수
- 선언적 함수

내부 함수

콜백 함수

자바스크립트에서 제공하는 전역 함수
- eval()
- parseInt()
- isNaN()

코어 객체 종류
- Array, Date, String, Math 등
-> Date는 월이 0부터 시작해

