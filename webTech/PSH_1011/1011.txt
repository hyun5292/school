웹 -  중간고사 화요일 오후?
그 Account 만든거 3가지 있지? 그거 중간고사 나오면 쓸 수 있냐는뎅
-> PSH_1011_3 ~ 5파일
------------------------------------------------
String 객체

Math 객체
- Math.random() : 0~1보다 작은 랜덤한 실수 리턴
- Math.floor(m)은 m의 소수점 이하를 제거한 정수 리턴
->random에*100하고 floor로 소수점 지워주면 완성

사용자 객체 만들기
new Object()

리터럴 표기법
- 중괄호를 이용하여 객체의 프로퍼티와 메소드 지정

프로토타입(클래스?)

객체와 반복문
- for in 반복문

객체관련 키워드

html 페이지와 자바스크립트 객체

HTML DOM
- 태그 하나가 노드

DOM 객체의 구성 요소
-> document.getElementById("firstP");








